///////////////////////////////////////////////////////////////////////////////////////////////////////////
///                                                                                                        
/// Copyright, 2021 PopcornSAR Co., Ltd. All rights reserved.                                              
/// This software is copyright protected and proprietary to PopcornSAR Co., Ltd.                           
/// PopcornSAR Co., Ltd. grants to you only those rights as set out in the license conditions.             
///                                                                                                        
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// AUTOSAR VERSION                   : R20-11
/// GENERATED BY                      : PARA, Adaptive Application Generator
///////////////////////////////////////////////////////////////////////////////////////////////////////////
/// GENERATED FILE NAME               : rportrearcurtain.cpp
/// SOFTWARE COMPONENT NAME           : RPortRearCurtain
/// GENERATED DATE                    : 2024-10-17 15:32:19
///////////////////////////////////////////////////////////////////////////////////////////////////////////
#include "katech/aa/port/rportrearcurtain.h"
 
namespace katech
{
namespace aa
{
namespace port
{
 
RPortRearCurtain::RPortRearCurtain()
    : m_logger(ara::log::CreateLogger("KATC", "PORT", ara::log::LogLevel::kVerbose))
    , m_running{false}
    , m_found{false}
{
}
 
RPortRearCurtain::~RPortRearCurtain()
{
}
 
void RPortRearCurtain::Start()
{
    m_logger.LogVerbose() << "RPortRearCurtain::Start";
    
    // regist callback
    ara::core::InstanceSpecifier specifier{"KATECH/AA/RPortRearCurtain"};
    auto handler = [this](ara::com::ServiceHandleContainer<eevp::control::proxy::SoaRcurtainProxy::HandleType> handles,
                          ara::com::FindServiceHandle findHandle) {
        this->Find(handles, findHandle);
    };
    
    // find service
    auto find = eevp::control::proxy::SoaRcurtainProxy::StartFindService(handler, specifier);
    if (find.HasValue())
    {
        m_logger.LogVerbose() << "RPortRearCurtain::Start::StartFindService";
    }
    else
    {
        m_logger.LogVerbose() << "RPortRearCurtain::Start::StartFindService::" << find.Error().Message();
    }
    
    // run port
    m_running = true;
}
 
void RPortRearCurtain::Terminate()
{
    m_logger.LogVerbose() << "RPortRearCurtain::Terminate";
    
    // stop port
    m_running = false;
    
    // clear consumer
    if (m_interface)
    {
        StopSubscribesoaRctnStatus();
        StopSubscribesoaRctnSwVersion();
        // stop find service
        m_interface->StopFindService(*m_findHandle);
        m_found = false;
        
        m_logger.LogVerbose() << "RPortRearCurtain::Terminate::StopFindService";
    }
}
 
void RPortRearCurtain::Find(ara::com::ServiceHandleContainer<eevp::control::proxy::SoaRcurtainProxy::HandleType> handles, ara::com::FindServiceHandle findHandle)
{
    // check finding handles
    if (handles.empty())
    {
        m_logger.LogVerbose() << "RPortRearCurtain::Find::Service Instances not found";
        return;
    }
    else
    {
        for (auto& handle : handles)
        {
            m_logger.LogVerbose() << "RPortRearCurtain::Find::Searched Instance::ServiceId =" << 
                                     handle.GetServiceHandle().serviceId << 
                                     ", InstanceId =" << 
                                     handle.GetServiceHandle().instanceId;
        }
    }
    
    // create proxy
    if (m_interface)
    {
        m_logger.LogVerbose() << "RPortRearCurtain::Find::Proxy is already running";
    }
    else
    {
        m_logger.LogVerbose() << "RPortRearCurtain::Find::Using Instance::ServiceId =" << 
                                 handles[0].GetServiceHandle().serviceId << 
                                 ", InstanceId =" << 
                                 handles[0].GetServiceHandle().instanceId;
        m_interface = std::make_shared<eevp::control::proxy::SoaRcurtainProxy>(handles[0]);
        m_findHandle = std::make_shared<ara::com::FindServiceHandle>(findHandle);
        m_found = true;
        // subscribe field notifications
        SubscribesoaRctnStatus();
        SubscribesoaRctnSwVersion();
    }
}
 
void RPortRearCurtain::SubscribesoaRctnStatus()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // 
        // RegistReceiversoaRctnStatus();
        
        // request subscribe
        auto subscribe = m_interface->soaRctnStatus.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPortRearCurtain::SubscribesoaRctnStatus::Subscribed";
        }
        else
        {
            m_logger.LogVerbose() << "RPortRearCurtain::SubscribesoaRctnStatus::" << subscribe.Error().Message();
        }
    }
}
 
void RPortRearCurtain::StopSubscribesoaRctnStatus()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->soaRctnStatus.Unsubscribe();
        
        m_logger.LogVerbose() << "RPortRearCurtain::StopSubscribesoaRctnStatus::Unsubscribed";
    }
}
 
void RPortRearCurtain::RegistReceiversoaRctnStatus()
{
    if (m_found)
    {
        // regist callback
        auto receiver = [this]() -> void {
            return ReceiveFieldsoaRctnStatusTriggered();
        };
    }
}
 
void RPortRearCurtain::ReceiveFieldsoaRctnStatusTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        if (m_interface->soaRctnStatus.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaRctnStatus.GetNewSamples([&](auto samplePtr) {
                RPortRearCurtain::ReadValuesoaRctnStatus(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPortRearCurtain::ReceiveFieldsoaRctnStatus::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogVerbose() << "RPortRearCurtain::ReceiveFieldsoaRctnStatus::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPortRearCurtain::ReceiveFieldsoaRctnStatusCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        if (m_found)
        {
            if (m_interface->soaRctnStatus.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaRctnStatus.GetNewSamples([&](auto samplePtr) {
                    RPortRearCurtain::ReadValuesoaRctnStatus(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPortRearCurtain::ReceiveFieldsoaRctnStatus::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogVerbose() << "RPortRearCurtain::ReceiveFieldsoaRctnStatus::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPortRearCurtain::ReadValuesoaRctnStatus(ara::com::SamplePtr<eevp::control::proxy::fields::soaRctnStatus::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    // process value logic
}
 
void RPortRearCurtain::SubscribesoaRctnSwVersion()
{
    if (m_found)
    {
        // regist receiver handler
        // if you want to enable it, please uncomment below code
        // 
        // RegistReceiversoaRctnSwVersion();
        
        // request subscribe
        auto subscribe = m_interface->soaRctnSwVersion.Subscribe(1);
        if (subscribe.HasValue())
        {
            m_logger.LogVerbose() << "RPortRearCurtain::SubscribesoaRctnSwVersion::Subscribed";
        }
        else
        {
            m_logger.LogVerbose() << "RPortRearCurtain::SubscribesoaRctnSwVersion::" << subscribe.Error().Message();
        }
    }
}
 
void RPortRearCurtain::StopSubscribesoaRctnSwVersion()
{
    if (m_found)
    {
        // request stop subscribe
        m_interface->soaRctnSwVersion.Unsubscribe();
        
        m_logger.LogVerbose() << "RPortRearCurtain::StopSubscribesoaRctnSwVersion::Unsubscribed";
    }
}
 
void RPortRearCurtain::RegistReceiversoaRctnSwVersion()
{
    if (m_found)
    {
        // regist callback
        auto receiver = [this]() -> void {
            return ReceiveFieldsoaRctnSwVersionTriggered();
        };
    }
}
 
void RPortRearCurtain::ReceiveFieldsoaRctnSwVersionTriggered()
{
    if (m_found)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        if (m_interface->soaRctnSwVersion.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
        {
            auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaRctnSwVersion.GetNewSamples([&](auto samplePtr) {
                RPortRearCurtain::ReadValuesoaRctnSwVersion(std::move(samplePtr));
            }));
            if (recv->HasValue())
            {
                m_logger.LogVerbose() << "RPortRearCurtain::ReceiveFieldsoaRctnSwVersion::GetNewSamples::" << recv->Value();
            }
            else
            {
                m_logger.LogVerbose() << "RPortRearCurtain::ReceiveFieldsoaRctnSwVersion::GetNewSamples::" << recv->Error().Message();
            }
        }
    }
}
 
void RPortRearCurtain::ReceiveFieldsoaRctnSwVersionCyclic()
{
    while (m_running)
    {
        std::lock_guard<std::mutex> lock(m_mutex);
        if (m_found)
        {
            if (m_interface->soaRctnSwVersion.GetSubscriptionState() == ara::com::SubscriptionState::kSubscribed)
            {
                auto recv = std::make_unique<ara::core::Result<size_t>>(m_interface->soaRctnSwVersion.GetNewSamples([&](auto samplePtr) {
                    RPortRearCurtain::ReadValuesoaRctnSwVersion(std::move(samplePtr));
                }));
                if (recv->HasValue())
                {
                    m_logger.LogVerbose() << "RPortRearCurtain::ReceiveFieldsoaRctnSwVersion::GetNewSamples::" << recv->Value();
                }
                else
                {
                    m_logger.LogVerbose() << "RPortRearCurtain::ReceiveFieldsoaRctnSwVersion::GetNewSamples::" << recv->Error().Message();
                }
            }
        }
        std::this_thread::sleep_for(std::chrono::seconds(1));
    }
}
 
void RPortRearCurtain::ReadValuesoaRctnSwVersion(ara::com::SamplePtr<eevp::control::proxy::fields::soaRctnSwVersion::FieldType const> samplePtr)
{
    auto value = *samplePtr.Get();
    // process value logic
}
 
} /// namespace port
} /// namespace aa
} /// namespace katech
 
/// EOF